DECLARE
    v_object_count NUMBER;
BEGIN
    -- Check for TABLE 'RDI_FRAME'
    SELECT COUNT(*) INTO v_object_count FROM all_tables WHERE owner = 'RDI' AND table_name = 'RDI_FRAME';

    IF v_object_count = 0 THEN
        EXECUTE IMMEDIATE '
    CREATE TABLE "RDI_FRAME" 
       (	"FRAME_ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
    	"FRAME_NO" VARCHAR2(100) NOT NULL ENABLE, 
    	"FRAME_NAME" VARCHAR2(100) NOT NULL ENABLE, 
    	"CREATED_BY" VARCHAR2(50), 
    	"CREATED_ON" DATE, 
    	"MODIFIED_BY" VARCHAR2(50), 
    	"MODIFIED_ON" DATE, 
    	 PRIMARY KEY ("FRAME_ID")
      USING INDEX  ENABLE, 
    	 CONSTRAINT "UQ_FRAME" UNIQUE ("FRAME_NO")
      USING INDEX  ENABLE
       ) ;
        ';
        DBMS_OUTPUT.PUT_LINE('TABLE "RDI_FRAME" created successfully.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('TABLE "RDI_FRAME" already exists.');
    END IF;
EXCEPTION
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('Error processing CREATE statement for ''CREATE TABLE "RDI_FRAME" ( "FRAME_ID" NUMBER GENERATED BY DE...'': ' || SQLERRM);
        RAISE;
END;
/
DECLARE
    v_object_count NUMBER;
BEGIN
    -- Check for TABLE 'RDI_LKP_COMPANYTYPE'
    SELECT COUNT(*) INTO v_object_count FROM all_tables WHERE owner = 'RDI' AND table_name = 'RDI_LKP_COMPANYTYPE';

    IF v_object_count = 0 THEN
        EXECUTE IMMEDIATE '
    CREATE TABLE "RDI_LKP_COMPANYTYPE" 
       (	"COMPANY_TYPE_ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
    	"COMPANY_TYPE_DESCRIPTION_EN" VARCHAR2(4000) NOT NULL ENABLE, 
    	"COMPANY_TYPE_CODE_EN" VARCHAR2(100) NOT NULL ENABLE, 
    	"CREATED_BY" VARCHAR2(50), 
    	"CREATED_ON" DATE, 
    	"MODIFIED_BY" VARCHAR2(50), 
    	"MODIFIED_ON" DATE, 
    	"COMPANY_TYPE_DESCRIPTION_AR" VARCHAR2(4000) NOT NULL ENABLE, 
    	"COMPANY_TYPE_CODE_AR" VARCHAR2(100) NOT NULL ENABLE, 
    	 PRIMARY KEY ("COMPANY_TYPE_ID")
      USING INDEX  ENABLE, 
    	 CONSTRAINT "UQ_COMPANY_TYPE" UNIQUE ("COMPANY_TYPE_CODE_EN")
      USING INDEX  ENABLE
       ) ;
        ';
        DBMS_OUTPUT.PUT_LINE('TABLE "RDI_LKP_COMPANYTYPE" created successfully.');
    ELSE
        DBMS_OUTPUT.PUT_LINE('TABLE "RDI_LKP_COMPANYTYPE" already exists.');
    END IF;
EXCEPTION
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('Error processing CREATE statement for ''CREATE TABLE "RDI_LKP_COMPANYTYPE" ( "COMPANY_TYPE_ID" NUMBE...'': ' || SQLERRM);
        RAISE;
END;
/